const input=
`8
4
3
6
8
7
5
2
1`.trim().split('\n');

const T=+input[0];
const data=input.slice(1);
console.log(data)
const stack:number[]=[]
const output:string[]=[]

for(let t=1;t<=T;t++){
    const cmd=data[t]
    switch (cmd){
        case: ('cmd'!==String(t)) stack.Number(t)
        break;
    }
}

console.log(data)