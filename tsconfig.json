{
  "compilerOptions": {
    /* 필수 설정 */
    "target": "ES6",
    "module": "ESNext",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "jsx": "react-jsx", // ✅ React 17+ 이상에서 import React 없이 JSX 사용 가능
    "moduleResolution": "Node",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,

    /* 엄격 모드 */
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "isolatedModules": true, // ✅ Vite/Webpack 등에서 필수

    /* 환경 충돌 방지 */
    "types": ["react", "react-dom"], // ✅ @types/node 자동 로딩 방지
    "noEmit": true
  },
  "include": ["src"]
}
